namespace $.$$ {
	$mol_test({
		// 'user test'( $ ) {
		// 	const talent_dig = $.$yuf_entity_test_unit.add({
		// 		id: 't1',
		// 		title: 'dig',
		// 	})
		// 	$mol_assert_equal(talent_dig.title(), 'dig')
		
		// 	const talent_fly = $.$yuf_entity_test_unit.add({
		// 		id: 't2',
		// 		title: 'fly',
		// 	})
		// 	$mol_assert_equal(talent_fly.title(), 'fly')
		
		// 	const user = $.$yuf_entity_test_user.add({
		// 		id: 'u1',
		// 		name: '1',
		// 		age: 22,
		// 		unit_ids: [ 't1' ]
		// 	})

		// 	const ids_full = $.$yuf_entity_test_user.search({ })
		// 	// $mol_assert_equal(ids_full, [ 'u1' ] )
		// 	$mol_assert_equal(user.unit_ids(), [ 't1' ])
		
		// 	user.unit_add('t2')
		// 	$mol_assert_equal(user.unit_ids(), [ 't1', 't2' ])
		// 	user.unit_remove('t1')
		// 	$mol_assert_equal(user.unit_ids(), [ 't2' ])
		
		// }
	})
}


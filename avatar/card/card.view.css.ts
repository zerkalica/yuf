namespace $.$$ {
	
	$mol_style_define( $yuf_avatar_card, {

		padding: $mol_gap.space,
		flex: {
			wrap: 'wrap',
		},
		justifyContent: 'start',
		alignItems: 'center',
		gap: $mol_gap.text,

	} )

}

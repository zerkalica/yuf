$yuf_avatar_card $mol_view
	sub /
		<= Avatar $yuf_avatar_stub
			name <= stub_name <= name \
			login <= login \
			url <= url \
			needle <= needle \
		<= Name $yuf_avatar_name
			name <= name
			login <= login
			needle <= needle

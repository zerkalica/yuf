namespace $.$$ {
	export class $yuf_portal extends $.$yuf_portal {
		
		static modals(next?: readonly $mol_view[]) {
			return next ?? []
		}

		override modals() { return this.$.$yuf_portal.modals() }
		
	}
}

namespace $ {
	export enum $yuf_session_oids_error_code {
		invalid_grant,
		invalid_client,
		unauthorized_client,
		unsupported_grant_type,
		access_denied,
		invalid_token,
		insufficient_scope
	}
}
